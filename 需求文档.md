# 我的世界基岩版语言键值翻译器需求文档

## 1. 文档信息

- **项目名称**：Minecraft Bedrock Language Key Translator
- **项目类型**：纯前端网页工具
- **目标用户**：Minecraft 基岩版资源包、行为包、附加包制作者与本地化维护者
- **文档状态**：第一版需求
- **目标平台**：现代桌面浏览器，优先支持 Chromium 系浏览器
- **界面技术栈**：MDUI 2.x Web Components，遵循 Material Design 3
- **核心原则**：本地处理优先、最少上传、保留原包结构、输出可直接导入游戏的资源包或目标语言文件
- **产品模式**：整包模式、单文件模式（两者互斥）

## 2. 项目背景

Minecraft 基岩版通常把本地化文件放在包内任意层级的 `texts/` 文件夹中：

```text
texts/
├── languages.json
├── en_US.lang
├── zh_CN.lang
└── ...
```

本工具明确分为两种互斥的处理模式：

- **整包模式**：上传 `.mcpack`、`.mcaddon` 或 `.zip`，递归定位任意层级的 `texts/` 文件夹，处理其中的 `languages.json` 和目标语言文件，保留原包结构并导出完整归档。
- **单文件模式**：一次上传一个 `.lang` 或 `.txt` 语言键值文件，按 `key=value` 解析，允许选择或推断源语言、选择目标语言，复用翻译、文本保护、核对、人工编辑和风险检查能力，直接下载目标 `.lang` 或 `.txt`。该模式不生成或修改 `languages.json`，第一版不承诺多文件同时处理。

两种模式共享解析、保护、翻译、核对和风险检查能力，但输入扫描和输出边界不同。

> 基岩版标准文件名通常是 `languages.json`，语言内容文件通常是 `语言代码.lang`。用户口中的 `language.json` 作为兼容性需求保留：工具可以识别、预览并按设置生成，但默认输出以游戏标准 `languages.json` 为准，避免生成游戏不识别的文件。

## 3. 目标与非目标

### 3.1 目标

1. 提供“整包模式”和“单文件模式”两个明确入口，避免输入和输出规则混用。
2. 整包模式支持上传 `.mcpack`、`.mcaddon`、`.zip`，递归识别任意层级 `texts/`，处理 `languages.json` 和目标语言文件并导出完整归档。
3. 单文件模式第一版一次上传一个 `.lang` 或 `.txt`，按 `key=value` 解析，不生成或修改 `languages.json`，直接下载目标文件。
4. 自动识别语言代码、键值数量、重复键和格式问题；单文件模式允许用户选择或根据文件名推断源语言。
5. 共享批量翻译、文本保护、风险检查、可筛选核对、人工编辑和失败重试能力。
6. 保留整包模式原始包内非语言文件和目录结构，并在浏览器支持时提供授权目录写入；单文件模式只输出目标语言文件。
7. 对翻译变量、格式代码、键名和换行等内容执行校验，降低破坏资源或语言文件的风险。

### 3.2 非目标

1. 不承诺直接修改未授权的本地文件夹；浏览器安全策略不允许网页静默写入任意目录。
2. 不解析或修改纹理、模型、动画、脚本、行为逻辑等非语言资源。
3. 不保证机器翻译达到人工出版质量。
4. 不把 `xnx3/translate` 的网页 DOM 翻译能力误当作本地化文件生成能力；本项目使用其文本/对象翻译接口或兼容服务层。
5. 第一版不实现在线账号、云端项目协作、版本控制和服务端数据库。

## 4. 用户角色

### 4.1 资源包作者

上传一个包，补充目标语言，生成可导入的翻译包。

### 4.2 翻译人员

查看源文案和机器译文，逐条修改、标记已核对，导出最终语言文件。

### 4.3 审核人员

筛选未核对、变量不一致、疑似未翻译或翻译失败的条目，完成验收。

## 5. 核心用户流程

### 5.1 模式选择

1. 用户在 `mdui-tabs` 中选择“整包模式”或“单文件模式”，两种模式互斥。
2. 切换模式前，如果当前项目存在未导出的改动，必须提示用户确认。
3. 模式选择决定可接受的文件类型、扫描范围和导出格式，但两种模式共享翻译与核对工作台。

### 5.2 整包模式导入与扫描

1. 用户拖放或选择 `.mcpack`、`.mcaddon`、`.zip`。
2. 工具读取归档目录，不执行其中的脚本或文件。
3. 递归查找目录名大小写不敏感且路径边界明确的 `texts/` 文件夹。
4. 对每个 `texts/` 文件夹列出：所在路径、所属包、语言文件、清单文件和异常。
5. 若找到多个 `texts/` 文件夹，用户可分别选择要处理的分组；每个分组独立选择源语言文件和目标语言。
6. 已存在的目标语言文件对应选项显示为禁用状态，不允许选择该文件作为生成目标。
7. 若没有找到 `texts/`，显示可操作错误，并允许用户重新上传。

### 5.3 单文件模式导入与配置

1. 用户拖放或选择一个 `.lang` 或 `.txt` 文件；第一版不同时处理多个文件。
2. 工具只读取该文件，不扫描其所在目录，也不读取或生成 `languages.json`。
3. 优先从文件名中的语言代码推断源语言；无法可靠推断时要求用户选择。
4. 用户选择目标语言代码，并可指定目标文件扩展名；默认沿用源文件扩展名。
5. 导入后显示文件名、编码、键数量、注释数量、空值、重复键和解析错误。

### 5.4 语言识别与文件管理（按模式区分）

1. 整包模式优先读取每个 `texts/` 分组中的 `languages.json`；兼容读取 `language.json`，并标记其为非标准清单。
2. 整包模式扫描每个 `texts/` 分组内文件名形如 `xx_YY.lang` 或语言键值格式的 `.txt` 文件，补充清单中未声明但实际存在的语言。
3. 单文件模式仅根据当前文件名推断语言代码，不因文件扩展名为 `.txt` 而假定语言。
4. 识别语言代码，例如 `en_US`、`zh_CN`、`ja_JP`。
5. 展示语言或当前文件的：文件路径/文件名、键数量、空值数量、重复键数量、解析错误数量。
6. 整包模式默认把 `en_US.lang` 作为源语言候选；如果不存在，则选择键数量最多且有效的语言作为候选，并允许用户更改。
7. 单文件模式推断失败时必须由用户选择源语言，且源语言和目标语言不得相同。

### 5.5 整包模式生成或更新语言清单

仅整包模式执行语言清单处理，且每个 `texts/` 分组独立维护自己的清单，工具提供两种策略：

- **仅补齐清单**：只将实际存在的 `.lang` 文件加入清单。
- **准备目标语言**：将用户选择的目标语言加入清单，并在目标语言文件生成后确认清单同步更新。

标准输出示例：

```json
["en_US", "zh_CN", "ja_JP"]
```

若原包存在 `language.json`，默认保留原文件，不覆盖；界面提示用户同时生成标准 `languages.json`。用户可显式开启“兼容输出”后额外生成 `language.json`，但该文件不作为游戏标准文件保证。

### 5.6 选择翻译任务

整包模式按 `texts/` 分组分别配置任务。用户可以在每个分组内选择一个或多个已有语言文件作为源文件，并选择一个或多个目标语言；每个目标语言分别生成翻译任务。

目标语言列表按当前分组已有语言文件动态计算：不存在对应语言文件的目标语言可以多选，已存在对应语言文件的选项必须禁用并标注“已存在”；不允许通过覆盖已有文件执行翻译，必须先删除该文件并完成二次确认。每个选中的目标语言独立生成语言文件、翻译状态和校验结果。

单文件模式只选择一个源文件和一个目标语言。

通用选项：

- 源语言。
- 翻译范围：全部键、仅缺失键、仅空值、仅未核对项。
- 是否覆盖已有目标语言译文，默认不覆盖。
- 是否保留注释。
- 是否启用术语表。

工具根据源语言键集合生成任务：

- 新增键：源语言有、目标语言没有。
- 空译文：目标语言存在但值为空。
- 待复核：已有译文但尚未标记核对。
- 多余键：目标语言有、源语言没有。
- 未翻译疑似项：目标值与源值完全相同，且不属于数字、占位符、专有名词或用户豁免规则。

### 5.7 翻译与编辑

1. 工具按批次提交待翻译文本。
2. UI 显示总数、已完成、失败、跳过和当前请求状态。
3. 机器译文写入内存中的项目状态，不立即覆盖原始上传文件。
4. 用户可以直接编辑译文、撤销单条修改、批量标记已核对。
5. 翻译失败的条目保留源文案，并明确标记失败原因。
6. 页面刷新前可导出临时工作文件；第一版不要求自动云端保存。

### 5.8 核对、校验与导出

1. 用户进入“核对”视图，查看并处理翻译风险。
2. 默认优先展示高风险条目：变量不一致、空译文、失败、未翻译疑似项、重复键。
3. 用户可以按语言、状态、关键词、文件路径和风险类型筛选。
4. 每条显示键名、源语言值、目标语言值、变量检查结果和核对状态。
5. 用户确认后执行全量校验。
6. 校验通过或用户确认忽略警告后，按当前模式输出结果。
7. 整包模式导出文件默认命名为：`原文件名-translated.zip`。`.mcpack` 和 `.mcaddon` 第一版统一按 ZIP 导出，并允许用户手动改扩展名。
8. 单文件模式默认命名为：`目标语言代码-原文件名`；扩展名默认沿用源文件扩展名，用户可在导出前切换 `.lang` 或 `.txt`。
9. 单文件模式只下载一个目标语言键值文件，不生成或修改任何语言清单。

## 6. 功能需求

### 6.1 文件导入

- 整包模式支持 `.mcpack`、`.mcaddon`、`.zip`；单文件模式支持 `.lang`、`.txt`，第一版一次仅一个文件。
- 整包模式识别到的每个 `.lang`/`.txt` 语言键值文件都归属于所在 `texts/` 分组，不能跨分组合并。
- 支持拖放，扩展名大小写不敏感；文件类型不可信，整包模式必须以归档内容为准识别。
- 整包模式文件大小上限默认 200 MB，允许后续配置；对损坏 ZIP、路径穿越、重复文件名和不支持的压缩格式给出明确提示。
- 禁止执行归档内任何脚本、HTML 或可执行文件。
- 单文件模式不读取同目录其他文件，源语言优先由文件名推断，推断失败时必须由用户选择；目标语言必须由用户明确选择。

### 6.2 `texts/` 定位

扫描规则：

- 路径分隔符统一为 `/`。
- 目录名 `texts` 大小写不敏感。
- 只识别目录本身，不把 `texts_backup` 当作目标目录。
- 支持根目录、嵌套资源包目录、行为包目录和多包 `.mcaddon`。
- 多个 `texts/` 文件夹必须按大类型独立分组展示并展示路径，不能静默合并不同包的数据。

### 6.3 `.lang` 解析

解析要求：

- 使用 UTF-8 优先，必要时处理 UTF-8 BOM。
- 按行解析 `key=value`。
- 首个等号作为键值分隔符，值中允许继续出现等号。
- 识别 `##` 注释并在保留注释模式下原样保留可安全部分。
- 保留原始键顺序，新增键追加到文件末尾。
- 检测空键、重复键、无等号行、不可解码内容和空值。
- 允许值包含 Minecraft 占位符，例如 `%s`、`%1`、`%2`。
- 不对值进行未经用户确认的自动转义或格式重排。

### 6.4 文本保护

翻译前识别并保护以下内容，翻译后恢复：

- `%s`、`%1`、`%2` 等格式占位符。
- Minecraft 格式代码与颜色代码，例如 `§a`、`§r`。
- 明确的命令、资源标识符、命名空间和键名。
- 用户配置的术语和不可翻译词。

校验规则：

- 源值与目标值的占位符集合必须一致，顺序变化需警告。
- 格式代码缺失或新增需警告。
- 目标值为空时标记错误。
- 键名不得被翻译或改变。
- 值不得包含未处理的换行；如源文件通过转义表达换行，必须保留其语义。

### 6.5 翻译服务对接

#### 推荐适配方式

建立前端 `TranslationProvider` 接口，默认实现为 `xnx3/translate` 兼容适配器。界面不直接耦合某个服务的内部细节。

逻辑请求模型：

```js
{
  from: "en_US",
  to: "zh_CN",
  texts: ["Stone", "Open settings"],
  glossary: {}
}
```

逻辑响应模型：

```js
{
  result: 1,
  from: "en_US",
  to: "zh_CN",
  text: ["石头", "打开设置"],
  info: ""
}
```

#### xnx3/translate 要求

- 优先使用其文本批量翻译方法，例如仓库示例中的 `translate.request.translateText`。
- 不依赖其 DOM 扫描、语言选择器或网页 UI 能力。
- 支持配置自定义翻译服务地址，以适配私有部署或代理服务。
- 不在源码中硬编码用户 API Key。
- 纯前端直连必须明确提示 CORS、网络可用性、服务限流和第三方数据传输风险。
- 默认只发送用户勾选的文本，不发送完整资源包和非语言文件。
- 接口异常时不丢失人工编辑内容，失败条目进入重试或手动编辑状态。
- 批量请求需要保持输入顺序，重复文本必须按原始索引正确回填。

#### 基岩版官方语言列表

目标语言（以及源语言候选）必须从以下 Minecraft 基岩版官方支持的 29 种语言中选择（来源：Microsoft Learn《Comprehensive List of Add-On Pack Contents》，与 Bedrock Wiki 的 Vanilla Languages 一致）：

| 语言代码 | 名称 | 翻译服务语言名 |
| --- | --- | --- |
| en_US | 英语（美国） | english |
| en_GB | 英语（英国） | english |
| de_DE | 德语（德国） | deutsch |
| fr_FR | 法语（法国） | french |
| fr_CA | 法语（加拿大） | french |
| it_IT | 意大利语（意大利） | italian |
| ja_JP | 日语（日本） | japanese |
| ko_KR | 韩语（韩国） | korean |
| zh_CN | 简体中文（中国大陆） | chinese_simplified |
| zh_TW | 繁体中文（中国台湾） | chinese_traditional |
| ru_RU | 俄语（俄罗斯） | russian |
| es_ES | 西班牙语（西班牙） | spanish |
| es_MX | 西班牙语（墨西哥） | spanish |
| pt_BR | 葡萄牙语（巴西） | portuguese |
| pt_PT | 葡萄牙语（葡萄牙） | portuguese |
| nl_NL | 荷兰语（荷兰） | dutch |
| bg_BG | 保加利亚语 | bulgarian |
| cs_CZ | 捷克语 | czech |
| da_DK | 丹麦语 | danish |
| el_GR | 希腊语 | greek |
| fi_FI | 芬兰语 | finnish |
| hu_HU | 匈牙利语 | hungarian |
| id_ID | 印尼语 | indonesian |
| nb_NO | 挪威语 | norwegian |
| pl_PL | 波兰语 | polish |
| sk_SK | 斯洛伐克语 | slovak |
| sv_SE | 瑞典语 | swedish |
| tr_TR | 土耳其语 | turkish |
| uk_UA | 乌克兰语 | ukrainian |

要求：

- 界面中的源语言和目标语言选择列表统一使用上表，展示格式为 `语言代码 · 中文名`。
- 地区变体共享同一翻译语言（如 `en_US` 与 `en_GB` 都映射到 `english`）：同语言变体之间翻译时直接复制源文本，仍按独立语言代码生成目标文件。
- `xnx3/translate` 不识别 `xx_YY` 形式的基岩语言代码，发送请求前必须完成代码到语言名的转换；映射失败时不得静默丢弃译文，必须提示用户。
- 上传包内出现的非官方语言代码（自定义语言，如 `xx_XX`）不进入目标语言下拉，但解析和导出时保留原样，不删除、不改名。

### 6.6 翻译核对

状态枚举：

- `pending`：待翻译。
- `translated`：已有机器译文。
- `edited`：用户手动修改过。
- `reviewed`：用户标记已核对。
- `warning`：存在校验警告。
- `error`：翻译或解析失败。
- `skipped`：用户跳过。

风险检测：

- 缺失键。
- 空译文。
- 重复键。
- 占位符数量或类型不一致。
- 格式代码不一致。
- 源值与目标值完全相同。
- 目标语言存在源语言没有的多余键。
- 译文疑似包含接口错误文本。

核对操作：

- 单条编辑。
- 单条标记/取消核对。
- 批量标记当前筛选结果。
- 快速跳转到上一条/下一条风险项。
- 显示源值和目标值的字符差异。
- 导出核对报告为 JSON 或 CSV，作为可选功能。

翻译结果列表：

- 翻译成功后必须单独显示“已翻译文件”列表，集中放置当前已生成的目标语言文件，不与源文件列表混排。
- 每个已翻译文件条目默认折叠；展开后提供一个覆盖完整文件内容的整体编辑框（含键名、注释和译文），用于后期人工校对。
- 校对输入实时保存到待导出内容；整包模式在导出资源包时生效，单文件模式在下载时生效。
- 结果列表外观必须与上方的语言文件列表保持一致（同边框、同圆角、同信息密度）。
- 单文件模式翻译完成后不再直接下载，改为先显示结果校对列表，由用户确认后手动下载。
- 整包模式重复翻译同一目标语言时替换该语言的原有结果，不产生重复条目；重渲染列表后必须恢复各分组已选择的源文件和目标语言。

### 6.7 输出

#### 整包模式

- 原始文件未修改，所有写入在内存中的新归档完成后一次性导出。
- 保留未处理的所有文件和目录结构。
- 写入或更新选定 `texts/` 目录下的 `languages.json`。
- 写入目标语言 `.lang` 文件；若用户确认删除已有语言文件，则从对应分组的归档内容和语言清单中移除。
- 默认使用 UTF-8、换行符 LF；提供“保持源文件换行风格”选项。
- 归档内文件路径使用 `/`，禁止输出 `../` 或绝对路径。

#### 单文件模式

- 原始文件未修改，直接生成并下载一个目标语言文件。
- 默认保持源文件的 `.lang` 或 `.txt` 扩展名，也允许用户在导出前切换扩展名。
- 默认文件名包含目标语言代码；不生成、不读取、不修改 `languages.json` 或 `language.json`。
- 保留键顺序和可安全保留的注释，使用 UTF-8 编码；换行符遵循用户选择。

#### 通用导出确认

- 生成前显示变更摘要：新增文件、修改文件、新增键、修改键、警告数量；单文件模式显示目标文件名和键数量。
- 校验存在未忽略的错误时禁止导出；仅有警告时要求用户明确确认。

## 7. 界面需求

### 7.1 页面结构

1. **顶栏与模式切换**：使用 `mdui-top-app-bar`；使用 `mdui-tabs` 明确切换“整包模式”和“单文件模式”，两种模式互斥且切换前提示未导出改动。
2. **导入页**：使用 `mdui-card` 承载上传区、格式说明、隐私提示和最近项目状态。整包模式显示 `.mcpack/.mcaddon/.zip`，单文件模式显示 `.lang/.txt`。
3. **扫描/配置页**：使用 `mdui-card`、`mdui-select`、`mdui-text-field`、`mdui-checkbox`/`mdui-switch` 展示按 `texts/` 路径分组的扫描结果、语言文件管理、可选源文件/目标语言、范围、覆盖策略和服务设置；每个语言文件条目提供删除按钮，删除必须由 `mdui-dialog` 二次确认；目标语言文件已存在时禁用对应翻译选项；单文件模式隐藏 `texts/`、删除和清单操作。
4. **翻译工作台**：使用 `mdui-card`、`mdui-chip`/`mdui-badge`、`mdui-linear-progress` 展示任务统计、状态、进度、键值表和编辑区。
5. **核对页**：使用 `mdui-text-field`、`mdui-select`、`mdui-checkbox`/`mdui-switch` 提供风险筛选、差异查看和审核状态。
6. **导出反馈**：使用 `mdui-dialog` 展示变更摘要、校验结果和覆盖/写入确认；使用 `mdui-snackbar` 提示成功、失败和可操作建议。整包模式提供完整归档导出，单文件模式提供目标文件下载。

### 7.2 MDUI 2.x 与可访问性约束

- 界面强制使用 MDUI 2.x 标准 Web Components，并遵循 Material Design 3；不得以自绘组件替代上述组件映射。
- 使用 Design Tokens/CSS 自定义属性统一颜色、间距、圆角、字体和状态语义；布局必须响应式，适配桌面和窄屏。
- 支持深色模式，并尊重系统 `prefers-color-scheme`；状态不得仅依赖颜色，需有文字或图标辅助。
- 所有交互控件具备键盘可操作性、可见焦点、合理焦点顺序、标签和必要的 ARIA 语义；对话框打开后管理焦点，错误与进度变化可被辅助技术感知。

### 7.2 交互要求

- 所有长任务显示进度和可取消状态。
- 错误信息必须包含处理对象和建议动作。
- 不使用仅颜色表达状态；状态需有文字或图标辅助。
- 表格支持固定键名列、横向滚动和虚拟列表，以适应大型语言文件。
- 编辑中离开页面或重新导入时提示未导出的改动。
- 对“覆盖已有翻译”和“写回目录”提供明确确认。

## 8. 纯前端技术约束

### 8.1 本地处理

- 归档读取、解析、合并和导出全部在浏览器完成。
- 使用成熟的前端 ZIP 库处理 ZIP 读写；不得手写压缩算法。
- 大文件处理尽量使用流式或分批处理，避免一次性复制多个完整字符串。
- 项目状态默认只保存在内存；可选使用 IndexedDB 保存草稿。

### 8.2 目录写入

- 优先使用 File System Access API 的目录句柄，在用户明确授权后写入。
- 不支持该 API 的浏览器必须提供 ZIP 下载。
- 不能声称“直接修改本地文件夹”在所有浏览器可用。
- 写入前展示将修改的相对路径列表，并要求用户确认。

### 8.3 网络与隐私

- 本地解析不需要网络。
- 只有执行机器翻译时才发起网络请求。
- 设置页显示当前服务地址和数据发送说明。
- 允许用户关闭在线翻译，改为手动编辑或导入已有翻译结果。
- CORS、网络失败、限流和服务响应格式错误均需可见化。

## 9. 数据模型

### 9.1 项目

```js
{
  mode: "archive" | "single-file",
  name,
  sourceFileName,
  archiveType, // 单文件模式为空
  inputExtension, // `.lang` 或 `.txt`
  entries: [],
  languageFiles: [],
  textFolderGroups: [],
  selectedTextFolders: [],
  languages: [],
  sourceLanguage,
  targetLanguages: [],
  changes: [],
  warnings: []
}
```

### 9.2 语言文件与分组

```js
{
  groupPath,
  path,
  fileName,
  language,
  deleted: false
}
```

### 9.3 文本条目

```js
{
  key,
  sourceValue,
  translations: {
    zh_CN: {
      value,
      status,
      reviewed,
      issues: []
    }
  },
  sourcePath,
  order,
  comment
}
```

### 9.4 变更记录

```js
{
  path,
  action, // `added` | `changed` | `removed`
  groupPath,
  addedKeys: 0,
  changedKeys: 0,
  removedKeys: 0
}
```

## 10. 验收标准

### 10.1 导入与识别

- 整包模式可成功导入有效 `.mcpack`、`.mcaddon` 和 `.zip`。
- 整包模式能识别根目录和任意嵌套层级的 `texts/`，且多个候选不会被静默混合。
- 单文件模式可成功导入一个 `.lang` 或 `.txt`，不读取同目录其他文件。
- 无 `texts/`、损坏归档、非法路径和单文件解析错误能给出明确处理建议。

### 10.2 语言处理

- 整包模式能读取标准 `languages.json`，并在缺少清单时生成标准清单。
- 整包模式能根据实际语言键值文件补齐缺失语言，并将每个 `texts/` 文件夹独立分组展示。
- 整包模式可从每个分组中选择源语言文件，并同时多选尚不存在语言文件的目标语言；已有目标语言文件对应选项不可用。
- 整包模式删除语言文件前必须二次确认，取消后原文件不变。
- 两种模式均能识别重复键、空值和非法行。
- 单文件模式能正确推断或手动选择源语言，并选择一个目标语言。
- 语言文件键顺序在导出后保持稳定。

### 10.3 翻译与核对

- 整包模式支持一次将选定源语言文件批量翻译为多个尚未存在的目标语言，并分别保留翻译状态和校验结果。
- 翻译失败不会覆盖原文或丢失人工编辑。
- 重复源文本的批量结果不会错位。
- 占位符和格式代码不一致时能拦截或明确警告。
- 用户可逐条编辑并标记核对。
- 能筛选全部高风险条目。

### 10.4 导出

- 整包模式导出的 ZIP 可重新导入工具并正确识别，原包非语言文件保持不变，`languages.json` 与目标 `.lang` 内容一致。
- 单文件模式直接下载目标 `.lang` 或 `.txt`，且不生成或修改 `languages.json`。
- 两种模式导出前均显示变更摘要和校验结果；不支持目录写入的浏览器仍可正常下载结果。

## 11. 测试要求

至少提供以下可运行检查：

1. `.lang`/`.txt` 文件正常解析与序列化。
2. 首个等号、注释、BOM、空值和重复键处理。
3. `languages.json` 缺失、存在、非法 JSON 和非标准 `language.json` 处理；单文件模式确认不生成或修改清单。
4. 整包模式语言文件按 `texts/` 路径分组展示、源文件可选、多个未存在目标语言可同时选择、已有目标文件选项禁用并标注“已存在”、删除需二次确认。
5. 根目录与嵌套目录中的 `texts/` 扫描。
6. 多个 `texts/` 文件夹按各自路径独立分组处理，不静默合并不同节点的数据。
7. 整包模式源语言文件选择、已有目标语言文件禁用翻译选项和语言文件删除二次确认。
8. 单文件模式一次一个文件、文件名源语言推断/手动选择和目标语言选择。
9. 占位符、格式代码和键名保护。
10. 重复文本翻译结果按索引回填。
11. 翻译接口失败、超时和部分批次失败。
12. 整包模式生成语言文件和清单后重新打包并再次读取；单文件模式直接下载目标文件。
13. 目录写入不可用时整包模式回退到 ZIP 下载。
14. MDUI 模式页签、响应式布局、深色模式、键盘导航和必要的可访问性语义。

## 12. 风险与决策

| 风险 | 影响 | 处理方式 |
|---|---|---|
| 浏览器禁止静默写本地目录 | 无法直接修改用户目录 | 使用目录授权 API，统一提供 ZIP 下载 |
| `xnx3/translate` 服务地址或返回格式变化 | 翻译不可用 | Provider 适配层、响应校验、允许自定义服务 |
| 跨域限制 | 浏览器请求失败 | 提供 CORS 说明、私有代理/自部署配置、手动编辑兜底 |
| 大型原版语言文件 | 页面卡顿、内存占用高 | 分批解析、虚拟列表、批量翻译 |
| 基岩版格式差异 | 游戏内显示异常 | 保留原文格式、严格占位符校验、导出前检查 |
| `.mcaddon` 含多个包 | 写错资源包 | 展示包树和 `texts/` 路径，要求用户选择处理节点 |
| 用户误覆盖已有译文 | 数据损失 | 默认不覆盖，覆盖前确认，导出前显示差异 |
| 单文件扩展名与内容格式不一致 | 目标文件被误识别 | 默认沿用源扩展名，切换扩展名时明确提示 |

## 13. 第一版范围建议

### 13.1 第一版必须完成

- **整包模式**：原始文件未修改，可同时选择多个尚未存在的目标语言，分别生成目标语言文件，导出完整归档并同步 `languages.json`。
- **单文件模式**：原始文件未修改，直接下载一个目标 `.lang` 或 `.txt`，不读取或生成 `languages.json`。
- 两种模式均共享翻译、保护、核对、人工编辑和风险检查；在线翻译遵循 `TranslationProvider` 与 `xnx3/translate` 设计。

### 13.2 第一版可以延后

- File System Access API 直接写回目录。
- CSV/JSON 核对报告导出。
- IndexedDB 草稿恢复。
- 术语库管理页面。
- 多人协作和云端保存。
- 原扩展名自动恢复与 Minecraft 安装联动。

## 14. 参考资料

- [xnx3/translate](https://github.com/xnx3/translate)
- [xnx3/translate README](https://github.com/xnx3/translate/blob/master/README.md)
- [Minecraft Bedrock Wiki: Intro to Text & Localization](https://wiki.bedrock.dev/text/text-intro)
- [Microsoft Minecraft Creator 文档：语言文件说明](https://github.com/MicrosoftDocs/minecraft-creator/blob/41bde2694d5d2759928004adf0ef58a28d14fe18/creator/Documents/ComprehensivePackContents.md)

## 15. 待确认问题

1. `language.json` 是否是现有工作流必须生成的自定义文件，还是对 `languages.json` 的口误？默认实现按标准文件处理。
2. 翻译服务是否允许直接从浏览器访问，还是必须通过用户自己的代理/私有部署？
3. 是否要求导出后保持 `.mcpack` / `.mcaddon` 原扩展名？
4. 是否需要同时处理行为包和资源包中的多个 `texts/` 文件夹？
5. 单文件模式后续是否需要支持一次上传多个 `.lang`/`.txt` 文件？第一版按单文件处理。
6. 是否需要支持 `.lang` 之外的 JSON 本地化文件作为输入？
7. 是否需要提供预置的 Minecraft 语言代码名称映射和自定义语言名称文件 `language_names.json`？
